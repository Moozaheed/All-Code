#include <iostream>
#include <cmath>
#include<bits/stdc++.h>
using namespace std;

int main() {

    freopen("distance3.in", "r", stdin);
    freopen("distance3.out", "w", stdout);

    double x,y;
    cin>>x>>y;

    double X=y/x,ans;

    if(x>0 && y>0)
    {
        ans=atan(X);
    }
    
    else if(x<0 && y>0)
    {
        ans=atan(X);
    }
    
    else if(x>0 && y>0)
    {
        ans=atan(X);
    }
    else if(x>0 && y>0)
    {
        ans=atan(X);
    }

}
